<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MedApp</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
</head>
<body class="bg-secondary">

<nav class="navbar navbar-expand-lg navbar-dark bg-info"> 
                <a href="/index.html" class="navbar-brand">Online Prescriptions</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle Navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a href="/index.html" class="nav-link">
                                            Home
                            </a>

                        </li>
                        <li class="nav-item">
                            <a href="/meds" class="nav-link">
                                            Inventory
                            </a>

                        </li>
                        <li class="nav-item">
                            <a href="/dashboard" class="nav-link">
                                        Summary Page
                            </a>

                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">
                                        Blog
                            </a>

                        </li>

                    </ul>
                </div>
            </nav>
            
{{! actual code }}
    <h1 style="text-align:center; color:white;">This is the edit meds page</h1>

    <form style="text-align:center;" action="/meds/edit/{{med.mid}}" method="post">
    <input type="text" name ="name" value="{{med.name}}" placeholder="medicine name"/>
    <input type="text" name ="count" value="{{med.count}}" placeholder="count"/>
    <input type="text" name ="brand" value="{{med.brand}}" placeholder="brand"/>

    <button class="btn btn-warning" type="submit">Save</button>


    </form>
</body>
</html>